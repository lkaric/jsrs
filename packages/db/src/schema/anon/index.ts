// Anonymous domain — ZKP-backed tables.
// No foreign keys cross into the auth or jobs domains.
// Only company_id (uuid) is shared as a loose public identifier.
// Implemented in Phase 3.
export {};
